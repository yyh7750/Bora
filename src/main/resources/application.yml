server:
  address: localhost
  port: 8080

spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://i8b301.p.ssafy.io/bora?serverTimezone=UTC&characterEncoding=UTF-8
    username: ssafy
    password: ssafy
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true
        format_sql: true
  cache: redis
  redis:
    host: i8b301.p.ssafy.io
    port: 8379
    password: b301
  profiles:
    include: oauth
  # swagger오류때문
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

cloud:
  aws:
    credentials:
      accessKey: AKIA4FYBPHWXAAKBP2MC
      secretKey: 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
    s3:
      bucket: woojin-test-bucket
      # dir: kwj
    region:
      static: ap-northeast-2
    stack:
      auto: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
