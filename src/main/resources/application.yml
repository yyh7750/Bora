server:
  address: localhost
  port: 8080

spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/bora?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: ssafy
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        globally_quoted_identifiers: true
        format_sql: true
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
    password: b301
  profiles:
    include: oauth
cloud:
  aws:
    credentials:
      accessKey: AKIA4FYBPHWXGYURRXEA
      secretKey: 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
    s3:
      bucket: woojin-test-bucket
      # dir: kwj
    region:
      static: ap-northeast-2
    stack:
      auto: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
